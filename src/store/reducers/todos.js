const initialState = {
    key: '',
    title: '',
    descr: '',
};

export default function todosReduser(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}
